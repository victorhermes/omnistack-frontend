// lib history e connected-react-router são as libs para conectar o react-router-dom ao redux
// dessa maneira é possível controlar as rotas

import { createBrowserHistory } from "history";

const history = createBrowserHistory();

export default history;
